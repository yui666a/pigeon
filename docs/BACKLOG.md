# Pigeon バックログ（2026-07-13 時点）

2026-07-13 の一斉実装（PR #87〜#100）完了時点での残タスク。各項目の背景は `docs/design/`（現役）または `docs/archive/specs/`（実装完了済み）の該当設計書の「制限」「将来」節に記載がある。

2026-07-12 追記: Gmail Web / iPhone・Mac 標準メールでできることを全て網羅する方針。標準アプリ互換の未着手項目を A系（高）/ B系（中）/ C系（低）として追加した。

## ✅ 2026-07-13 に完了した項目（PR #87〜#100）

| 旧# | 項目 | PR |
|-----|------|-----|
| 1 | Sentフォルダの同期 + UID後追い確定 | #89（APPENDUID は async-imap 0.11.2 制約で不可→Sent差分同期+message_idマージ+uid_confirmed 方式。Sentの削除/アーカイブのサーバー反映は uid_confirmed=1 行の解禁として将来課題） |
| 2 | 添付ファイルの送信 + リッチテキスト（TipTap） | #96（メールごと切替+既定形式は localStorage。設定画面統合は #14 で） |
| 3 | 複数選択の一括操作 | #88 |
| 4 | 下書き保存（ローカル v1） | #92（IMAP Drafts 同期は将来） |
| 5 | インライン画像（cid:）表示 | #94 |
| 6 | 通知の強化（件名プレビュー） | #97（クリック前面化は tauri-plugin-notification 2.3.3 のデスクトップ実装にクリックイベントが無くスコープアウト） |
| 7 | IDLE監視の自動復帰 | 実装済みと判明（PR #74 がカバー、記載漏れ） |
| 8 | 過去メールのバックフィル | #99 |
| 9 | 検索結果リスト内の j/k ナビ | #90 |
| 10 | 未分類スレッドの部分分類（スレッド追従の自動分類） | #98（却下は follow_exclusions トゥームストーンで尊重） |
| 15 | CIをスタックPRでも実行 | #87 |
| A1 | スター / フラグの表示・付与・解除 | #93 |
| A2 | 手動で未読に戻す | #95 |
| — | 取り込み時の意図しない既読化（FETCH を BODY.PEEK[] に修正） | #100（不具合修正。既に既読化された既存メールの未読復元は不可） |

## 🔴 優先度: 高（標準メールアプリ互換の残り）

| # | 項目 | メモ |
|---|------|------|
| A3 | 宛先オートコンプリート / アドレス帳 | 宛先はカンマ区切り手入力のみ。過去送受信アドレスからの候補表示。連絡先テーブル or 既存メールのアドレス集約 |
| A4 | 署名 | 設定・挿入ロジックが皆無。アカウント別署名＋compose時自動挿入。設定画面統合（#14）とセット |
| A5 | 任意フォルダ / ラベルの閲覧・切替 | INBOX決め打ち。Sent/Trash/Drafts や任意ラベルを開けない。`list_folders` は存在するがコマンド未公開。Sent同期(#89)で論理フォルダ基盤はできた |
| A6 | フォルダ / ラベル間の手動移動 | 「案件移動」はIMAPフォルダ移動ではない。IMAP COPY+移動のUI |

## 🟡 優先度: 中

| # | 項目 | メモ |
|---|------|------|
| B1 | 迷惑メール / スパム報告 | Junkフォルダ移動・スパム学習。未実装 |
| B2 | ユーザー定義のフィルタルール | 現状の自動振り分けはAI分類のみ。「差出人Xは自動でラベルY」等のルールベース振り分け |
| B3 | 添付のプレビュー | 現状はファイル名・DLボタンのみで開かず中身を見られない。画像/PDFのインラインプレビュー |
| B4 | 条件検索 / 詳細フィルタ | 現状は単一クエリ文字列のみ。`from:`・件名・日付・添付有無での絞り込み |
| B5 | カレンダー招待（.ics）の表示・応答 | 会議招待の参加/辞退。未実装 |
| B6 | スヌーズ / 後で通知 | Gmail・標準メール標準機能。未実装 |
| 17 | Sentメールの削除・アーカイブのサーバー反映 | #89 で uid_confirmed 基盤は導入済み。plan_delete/plan_archive を「folder=='Sent' && uid_confirmed で解禁」に拡張（設計書 2026-07-12-sent-sync-uidplus-design.md 参照） |
| 18 | plan_delete/plan_archive の共通化 | bulk_commands.rs と mail_commands.rs に複製がある（#88 レビュー記録。相互参照コメントで固定済み） |

## 🟢 優先度: 低・整理系

| # | 項目 | メモ |
|---|------|------|
| 11 | テーマ切り替え（ダーク/ライト） | 本体設計 Phase 6 |
| 12 | 添付ファイルの中身をAI分類材料に | 本体設計 Phase 6。クラウド送信ポリシーとセットで設計必要 |
| 13 | 添付キャッシュの削除ポリシー | メール削除時にDB行はCASCADEで消えるがキャッシュファイルが残る |
| 14 | 設定画面の統合 | LLM設定・クラウドルール・通知トグル・通知件名プレビュー・compose既定形式が別々の場所にある。汎用設定画面への統合 |
| 16 | 通知ON/OFF等の保存先 | 現在localStorage（pigeon.notifyNewMail / pigeon.notifySubjectPreview / compose既定形式）。設定画面統合時にsettingsテーブルへ |
| C1 | 送信予約 / 送信取り消し（Undo Send） | Gmailの定番。未実装 |
| C2 | 印刷 | メール本文の印刷。未実装 |
| C3 | エクスポート（.eml / mbox） | メールの書き出し。未実装 |
| C4 | 通知の出し分け | フォアグラウンド時は通知しない等。VIP/差出人フィルタ |
| C5 | vCard（連絡先添付）の取り込み | .vcf からアドレス帳へ。A3とセット。未実装 |
| C6 | ~~send_mail 添付パスの allowlist~~ | **2026-07-15 #138 で対応済み**（pick_attachment_files + ApprovedAttachments） |
| C7 | merge_duplicate_sent_rows の両方割り当てケース | #89 レビュー記録。重複Sent行の統合時、両行が別案件に割り当て済みだと drop 側の割り当てが警告なく消える稀ケース |
| C8 | SBOM 生成（リリース配布時） | 公開リリース配布を始める段階で、`cargo cyclonedx` と pnpm 依存から SBOM を CI 自動生成する。手書きはすぐ陳腐化するので今は作らない。依存は Cargo.lock / pnpm-lock.yaml で固定済み |
| C9 | ~~外部画像の表示オプトイン~~ | **2026-07-15 #160 で対応済み**（設計書 2026-07-15-external-image-optin-design.md。「画像を表示」→Rust経由フェッチ→data URI。非永続。送信者単位の常時許可・DNSリバインディング対策は将来課題） |
| C10 | ~~メール本文の sandbox iframe 隔離~~ | **2026-07-15 #159 で対応済み**（実機検証の結果 WKWebView は frame-src 'none' を srcdoc に適用せず CSP 変更不要。allow-scripts なしの srcdoc iframe で隔離） |

## 📌 Riskゲート（監査 #17 / ADR 0004）の進捗

2026-07-15 の #161/#164/#165 で Phase 4-4 まで完了（asyncバス・Sensitive抽出・ゲート本体・audit_log v15・approval_queue v16）。残り:

- 4-5: ~~classify / sync~~ / rescan のバス載せ替え → **sync / classify は 2026-07-20 の #210 で完了。rescan は残り**
- 5-1: ~~MCP server driver~~ → **2026-07-20 に完了**（#209 基盤 / #210 載せ替え / #211 CLI / 本 PR で MCP stdio サーバー）
- 5-2: 承認キューの消費UI・承認後再実行 → **未着手**
- 5-3: 常駐エージェント → 未着手

CLI / MCP driver の設計は `docs/design/2026-07-20-cli-mcp-driver-design.md`。

**5-2 は 5-1 の前提になっている。** CLI（非TTY）と MCP から Sensitive 操作を呼ぶと承認キューに積まれるが、承認する導線が無いため**エージェントからのメール送信・案件削除は完了できない**。Read / Reversible（同期・分類・検索・既読・アーカイブ・案件作成）は承認不要なので主要導線は成立する。5-1 が完了した今、エージェント運用で最初に詰まるのはここ。

### 5-4: GUI と CLI / MCP の同時実行（新規・2026-07-20）

現在、**GUI 起動中は CLI / MCP がデータを読めない**。`pigeon.lock` の flock により後から起動した側が弾かれる。

このロックは必須のものである。Stronghold は排他ロックを取らず、2 プロセスが同じスナップショットを開くと後から commit した側が先の書き込みを丸ごと上書きし、**エラーも警告もなくシークレットが消える**（2026-07-20 実測）。ロックを外す選択肢はない。

一方で運用上の制約は大きい。「アプリを閉じてから CLI を叩く」はエージェントに常用させる形と噛み合わない。MCP の `initialize` / `tools/list` は Registry だけで応答するため GUI 起動中でも接続できるが、`tools/call` は弾かれる。

解決策はローカル IPC（Unix domain socket 等）で、GUI が起動中なら CLI / MCP が GUI プロセスへ dispatch を転送する形。GUI 側に受け口を持たせ、`Ctx` の driver は転送元のものを使う（gate の判定を GUI 相当に格上げしない）。

設計時に「まず排他で作り、実際に困ったら IPC を検討する」と判断した経緯がある（`docs/design/2026-07-20-cli-mcp-driver-design.md` の排他制御節）。5-3（常駐エージェント）に着手する段階では前提になる。

2026-07-15 の #166 で Linux の secret-service 鍵保管も対応済み（ADR 0003 更新済み。master.key の廃止は Linux 正式配布時）。

## ⚠️ 引き継ぎ時の注意（開発運用）

- **`gh pr merge --delete-branch` はそのブランチをbaseにする子PRを自動クローズする**。スタックのマージは「親マージ → 子のbaseをmainへ変更 → 最後にブランチ削除」の順で
- **DBマイグレーションは番号昇順にマージすること**（逆順だと既存DBで後発の低番号ゲートが永久にスキップされる。2026-07-13 のマージで実際に順序制約が発生した）。現在 v16 まで使用済み
- **IMAP FETCH の本文取得は必ず `BODY.PEEK[]`**（RFC822/BODY[] は取得だけでサーバーに \Seen が付く。#100 の不具合。`FETCH_ITEMS_*` 定数と回帰テストで固定済み）
- `cargo fmt -- <file>` はファイル限定にならずクレート全体を整形する
- 並行エージェント開発時: DBマイグレーション番号の事前割り当て、`lib.rs` invoke_handler への挿入位置の分散。**並行実装は最大3体・検証フェーズ（cargo test/ビルド）は同時1〜2体まで**（2026-07-13 未明に6体並行でPCフリーズ）。`cargo test --lib -- --test-threads=4`・vitest `--maxWorkers 2` で負荷制限する
- `CARGO_TARGET_DIR` 共有はビルド高速化になるが、ソースの異なるworktreeが交互にビルドすると fingerprint が揺れてほぼ毎回再コンパイルになる。並行3体超では共有しない
- vitest は `include: src/**` 限定済み（#77）
- DB接続は `Mutex<Connection>` 単一。大量データでロック競合が見えたら接続プール化を検討
